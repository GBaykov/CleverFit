export interface StyledLinkProps {
    fontSize?: string;
    color?: string;
    to: string;
}

export interface CardInfoProps {
    children?: React.ReactNode;
    text: string;
    to: string;
    color?: string;
    fontSize?: string;
}
